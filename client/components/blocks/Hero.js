const Hero = ({ data }) => {
    const { image, alignContent, autoplay, buttons, media, mediaWidth, title, video, description } = data
    return (
    <div>
        Hello World
    </div>
)
}

export default Hero